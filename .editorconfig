# Helps editors use LF so Git does not fight your working tree on Windows.
# https://editorconfig.org
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.{cmd,bat}]
end_of_line = crlf

[*.md]
trim_trailing_whitespace = false
